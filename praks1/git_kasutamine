* root kasutajana: install installi
installi - apt-get install git
* tavakasutajana:
esmakordselt:

a) seadista - määra kasutjanimi, email, editor git config
git config --global user.name "Karl Uiga"
git config --global user.email karl.uiga@voco.ee
git config --global core.editor nano

b) loo kataloog, kus hakkad skriptima
mkdir skriptimine

c) sisene antud kaust
cd skriptimine

d) tekita lokaalne repositoorium git süsteemi jaoks antud kaustas
git init

remote seadistamine:
e) git remote add nimi github:address
git remote add skriptimine git@github.com:kasutajanimi/reponimi.git

f) git remote kontroll
git remote -v
